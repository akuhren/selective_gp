from setuptools import setup, find_packages

setup(
    name='selective_gp',
    version='1.0.0',
    author='Anders Kirk Uhrenholt',
    description='Code accompanying the paper "Probabilistic Selection of Inducing Points in Sparse Gaussian Processes".',
    packages=find_packages(),
)
